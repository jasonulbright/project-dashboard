# Build artifacts
bin/
obj/
publish/
TestResults/
*.pdb

# Editor state
.vs/
.vscode/
.idea/
*.user
*.suo

# Local working files (never shipped)
*.local.*
*.private.*

# Installer build artifacts (sources are tracked: branding/*.svg, installer/*.nsi, installer/*.ps1)
installer/payload/
installer/ProjectDashboard-Portable-*/
installer/*.exe
installer/*.zip
installer/*.zip.partial
branding/*.png

# Local working state (never shipped)
.claude/
CLAUDE.md
userinput.md

# The *.local.* pattern requires a dot after "local"; directories need their own rule
*.local/

# OS
Thumbs.db
Desktop.ini
.DS_Store
